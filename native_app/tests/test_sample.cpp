#include <gtest/gtest.h>

/**
 * PUBLIC_INTERFACE
 * Basic sanity test to verify GoogleTest is configured and compiles/links correctly.
 * This also serves as a template for adding future tests.
 */
TEST(Sanity, TrueIsTrue) {
    EXPECT_TRUE(true);
}
